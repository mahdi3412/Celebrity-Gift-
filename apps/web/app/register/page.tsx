"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
export default function Register(){const router=useRouter();const [message,setMessage]=useState("");const [loading,setLoading]=useState(false);
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMessage("");const form=new FormData(e.currentTarget);try{const r=await fetch("/api/auth/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({displayName:form.get("displayName"),email:form.get("email"),password:form.get("password"),role:form.get("role")})});const data=await r.json();if(!r.ok)throw new Error(Array.isArray(data.message)?data.message.join("، "):(data.message??"ثبت‌نام انجام نشد"));router.push("/dashboard");router.refresh();}catch(error){setMessage(error instanceof Error?error.message:"خطا در ثبت‌نام");}finally{setLoading(false);}}
return <main className="mx-auto max-w-lg px-6 py-16" dir="rtl"><h1 className="text-3xl font-bold">ساخت حساب</h1><p className="mt-2 text-neutral-600">حساب طرفدار یا کرییتور بسازید. احراز هویت برای استفاده از قابلیت‌های اصلی الزامی است.</p><form onSubmit={submit} className="mt-8 space-y-4"><input name="displayName" required minLength={2} maxLength={80} placeholder="نام نمایشی" className="w-full rounded-xl border p-3"/><input name="email" required type="email" placeholder="ایمیل" className="w-full rounded-xl border p-3"/><input name="password" required minLength={12} type="password" placeholder="رمز عبور (حداقل ۱۲ کاراکتر)" className="w-full rounded-xl border p-3"/><select name="role" className="w-full rounded-xl border p-3"><option value="fan">طرفدار</option><option value="creator">کرییتور</option></select><button disabled={loading} className="w-full rounded-xl bg-black p-3 text-white disabled:opacity-50">{loading?"در حال ساخت حساب…":"ثبت‌نام امن"}</button></form>{message&&<p className="mt-4 rounded-xl bg-red-50 p-4 text-red-700">{message}</p>}</main>}