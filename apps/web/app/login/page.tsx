"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
export default function Login(){const router=useRouter();const [msg,setMsg]=useState("");const [loading,setLoading]=useState(false);
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMsg("");const form=new FormData(e.currentTarget);try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:form.get("email"),password:form.get("password")})});const data=await r.json();if(!r.ok)throw new Error(data.message??"ورود انجام نشد");router.push("/dashboard");router.refresh();}catch(error){setMsg(error instanceof Error?error.message:"خطا در ورود");}finally{setLoading(false);}}
return <main className="mx-auto max-w-md px-6 py-16" dir="rtl"><h1 className="text-3xl font-bold">ورود</h1><p className="mt-2 text-neutral-600">نشست امن شما با cookieهای HttpOnly مدیریت می‌شود.</p><form onSubmit={submit} className="mt-8 space-y-4"><input name="email" required type="email" placeholder="ایمیل" className="w-full rounded-xl border p-3"/><input name="password" required type="password" placeholder="رمز عبور" className="w-full rounded-xl border p-3"/><button disabled={loading} className="w-full rounded-xl bg-black p-3 text-white disabled:opacity-50">{loading?"در حال ورود…":"ورود امن"}</button></form>{msg&&<p className="mt-4 rounded-xl bg-red-50 p-4 text-red-700">{msg}</p>}</main>}