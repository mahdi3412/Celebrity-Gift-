import {Injectable} from "@nestjs/common";import {DatabaseService} from "../database/database.service";import {PrivateFieldEncryptionService} from "../privacy/private-field-encryption.service";@Injectable() export class CreatorPrivateAddressService{constructor(private readonly db:DatabaseService,private readonly crypto:PrivateFieldEncryptionService){}async set(creatorId:string,address:string){const encrypted=this.crypto.encrypt(address.trim());await this.db.query("INSERT INTO creator_private_addresses(creator_id,encrypted_address) VALUES($1,$2) ON CONFLICT(creator_id) DO UPDATE SET encrypted_address=EXCLUDED.encrypted_address,updated_at=now()",[creatorId,encrypted]);return {saved:true};}async get(creatorId:string){const r=await this.db.query<{encrypted_address:string}>("SELECT encrypted_address FROM creator_private_addresses WHERE creator_id=$1",[creatorId]);const row=r.rows[0];return {address:row?this.crypto.decrypt(row.encrypted_address):null};}}