Database foundation.

schema.sql defines the first PostgreSQL domain model. Current demo services remain in-memory.

Next: ORM migrations, repositories, transaction boundaries, seed data and integration tests before production traffic.